#![forbid(unsafe_code)]

//! Cross-product vertical invariants for the single root workspace.

/// Marker used by integration tests to prove this package linked successfully.
pub const GRAPH_KIND: &str = "single-root-workspace";
